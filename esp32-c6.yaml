# esp32-c6.yaml

# Updated addressable_lambda update_interval and added fx_active global variable

addressable_lambda:
  update_interval: 150ms  # Changed from 16ms to 150ms

fx_active: true
# Other existing configurations go here...